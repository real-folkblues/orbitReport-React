import React from 'react';
import satData from './satData';

const Buttons = () =>
{
  filterByType, setSat, displaySats
  return (
    <div>
      <button>{sat} Orbit</button>
      <button>All Orbits</button>
      </div>
  );
};

export default Buttons;